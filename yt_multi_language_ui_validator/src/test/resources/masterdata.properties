#Url = https://www.youtube.com/
#userAgent = Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/119.0.6045.200 Safari/537.36
#headless=false
#startMaximized=true
#disableGpu=false


# src/test/resources/utildata.properties

# Base application URL used by tests
base.url = https://www.youtube.com/

# Default locale for tests
default.locale = en-US

# -------------------------
# Browser / WebDriver
# -------------------------
# chrome | firefox | remote
browser = chrome

# If true, Chrome will start in headless mode (CI often uses true)
chrome.headless = false

# If using a remote grid, set use.remote.webdriver = true and remote.webdriver.url accordingly
use.remote.webdriver = false
remote.webdriver.url = 

# Optional direct path to a browser binary (leave empty to use system default)
chrome.binary.path = 

# Comma-separated extra Chrome args (no spaces after commas)
# Example: --no-sandbox,--disable-dev-shm-usage,--disable-gpu
chrome.additional.args = --no-sandbox,--disable-dev-shm-usage,--disable-gpu

# Start browser maximized (true/false)
chrome.start.maximized = true

# -------------------------
# Timeouts & Waits (seconds)
# -------------------------
# Implicit wait (seconds) - used sparingly, prefer explicit waits
implicit.wait = 5

# Default explicit wait used by WaitUtility / SafeActions (seconds)
explicit.wait = 10

# Page load timeout (seconds)
page.load.timeout = 60

# Script timeout (seconds)
script.timeout = 30

# SafeActions retry behavior
element.retry.attempts = 3
element.retry.wait.millis = 500

# -------------------------
# Retry / flaky-tests
# -------------------------
# How many times to retry a failed test (used by IRetryAnalyzer)
retry.max = 1

# If false, AssertionError is not retried (recommended)
retry.retryOnAssertionError = false

# -------------------------
# Screenshots & Reports
# -------------------------
# Where logs/run folder is created at run start; RunListener will normally set this.
logs.dir = logs

# Where screenshots will be saved (defaults to logs dir if changed by RunListener)
screenshots.dir = 

# Extent report output (if using Extent)
extentreport.enabled = true
extentreport.file =

# Take screenshots when a test fails
take.screenshots.on.failure = true
screenshots.format = png


# -------------------------
# Feature toggles / behavior
# -------------------------
# If true, SafeActions will fall back to JS click when native click fails
use.javascript.click.fallback = true

# If true, attempt to stop page load and refresh when page load appears stuck
stop.page.load.on.hang = true






# -------------------------
# CI hints (samples; override in CI)
# -------------------------
# Example: override at runtime with -Dchrome.headless=true or set env var CHROME_HEADLESS=true
# Example: mvn -Dconfig.file=/path/to/ci.properties -Dchrome.headless=true test
